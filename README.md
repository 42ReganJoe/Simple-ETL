# Simple-ETL
Install dependencies: pip install -r requirements.txt

Generate the mock data: python generate_mock_data.py (This creates the raw .csv and .json files).

Run the ETL pipeline: python etl_pipeline.py
